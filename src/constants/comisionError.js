export const comisionNoEncontrada = (cid) => {
  return `Comisión no encontrada:

        * Información requerida: 
        comisión no encontrada para el id ${cid},
        
         `;
};
export const existeAlumno = (uid) => {
  return `Alumno existe en la comisión:

        * Información requerida: 
        El alumno con id ${uid} ya existe en la comisión,
        
         `;
};
